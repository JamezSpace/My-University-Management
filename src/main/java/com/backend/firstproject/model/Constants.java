package com.backend.firstproject.model;

public class Constants {
    public enum Gender {
        MALE,
        FEMALE
    }

    public enum AdmissionStatus {
        REJECTED,
        PENDING,
        ACCEPTED
    }
}